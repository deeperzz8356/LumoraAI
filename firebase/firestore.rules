rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Merge this collection rule into the existing project rules before deploy.
    match /template_catalogs/{catalog} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.admin == true;
    }
  }
}
