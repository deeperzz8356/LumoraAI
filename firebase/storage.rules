rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Template previews are public media; only administrators may replace them.
    match /template-media/{path=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.admin == true;
    }
  }
}
